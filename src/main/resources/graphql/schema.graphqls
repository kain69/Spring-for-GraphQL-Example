type Query {
    users: [User]
}

type User {
    id: ID!
    name: String!
    posts: [Post]
}

type Post {
    id: ID!
    title: String!
    comments: [Comment]
}

type Comment {
    id: ID!
    text: String!
}